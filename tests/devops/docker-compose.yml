version: '3.8'

services:
    php:
        build:
            context: ./php-build
        restart: 'no'
        volumes:
            - '../..:/app'
        environment:
            XDEBUG_MODE: 'coverage'
        working_dir: '/app'
